apiVersion: v1
kind: Service
metadata:
  name: task-api
spec:
  type: {{ .Values.taskApi.service.type }}
  selector:
    app: task-api
  ports:
    - port: {{ .Values.taskApi.service.port }}
      targetPort: {{ .Values.taskApi.port }}
      nodePort: {{ .Values.taskApi.service.nodePort }}
